## Science Class Web Algorithm

Input file sample.txt

The input format is as following:

studentid	questionid	stepid

The output format is as following:

{((qid,qsid),(qid,qsid)):similarity, ...}
{(stid,stid):similarity, ...}
